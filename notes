Experimental loop levels

    For each dataset:

        For each balancing method:
            1. Balance the dataset
            2. Save the balanced dataset
            3. Create plots
            4. Save plots

            For each classic regression model:
                For each parameter combination:
                    1. Train the model
                    2. Save the model
                    3. Calculate the metrics
                    4. Save the results

            For each neural network regression model:
                For each depth of the network:
                    1. Train the model
                    2. Save the model
                    3. Calculate the metrics
                    4. Save the results